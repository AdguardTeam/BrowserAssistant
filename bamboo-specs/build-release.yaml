---
version: 2
plan:
  project-key: ADGEXT
  key: BARELEASESPECS
  name: browser assistant - build release

stages:
  - Build:
      manual: false
      final: false
      jobs:
        - Build

Build:
  key: BUILD
  other:
    clean-working-dir: true
  tasks:
    - checkout:
        force-clean-build: true
    - checkout:
        repository: extensions-private
        path: private
        force-clean-build: true
    - script:
        interpreter: SHELL
        scripts:
          - |-
            # 'set' should be added to the beginning of each script to ensure that it runs with the correct options.
            #  -e: Exit immediately if any command exits with a non-zero status (i.e., if a command fails).
            #  -x: Print each command to the terminal as it is executed, which is useful for debugging.
            set -ex

            # Fix mixed logs
            exec 2>&1

            export DOCKER_BUILDKIT=1

            # Generate find-compatible .gitignore exclusions on the host (where .git is available).
            # The gitignore-excludes.txt file is included in the Docker context and consumed by
            # archive-source.sh inside Docker (where .git is not available).
            ./bamboo-specs/scripts/generate-find-excludes.sh gitignore-excludes.txt

            # source.zip is created inside Docker via archive-source.sh.
            docker build \
              --file Dockerfile \
              --progress plain \
              --build-arg TEST_RUN_ID=${bamboo.buildNumber} \
              --build-context private=private \
              --secret id=CREDENTIALS_PASSWORD,env=bamboo_extensionsPassphrase \
              --target build-release-output \
              --output type=local,dest=output \
              .
    - inject-variables:
        file: output/artifacts/build.txt
        scope: RESULT
        namespace: inject
    - any-task:
        plugin-key: com.atlassian.bamboo.plugins.vcs:task.vcs.tagging
        configuration:
          selectedRepository: defaultRepository
          tagName: v${bamboo.inject.version}
  final-tasks:
    - script:
        interpreter: SHELL
        scripts:
          - "./bamboo-specs/scripts/cleanup.sh output/artifacts/build.txt,output/artifacts/chrome.crx,output/artifacts/chrome.zip,output/artifacts/edge.zip,output/artifacts/firefox.zip,output/artifacts/update.xml,output/artifacts/source.zip"
  artifacts:
    - name: edge.zip
      location: output/artifacts
      pattern: edge.zip
      shared: true
      required: true
    - name: build.txt
      location: output/artifacts
      pattern: build.txt
      shared: true
      required: true
    - name: chrome.crx
      location: output/artifacts
      pattern: chrome.crx
      shared: true
      required: true
    - name: chrome.zip
      location: output/artifacts
      pattern: chrome.zip
      shared: true
      required: true
    - name: firefox.zip
      location: output/artifacts
      pattern: firefox.zip
      shared: true
      required: true
    - name: update.xml
      location: output/artifacts
      pattern: update.xml
      shared: true
      required: true
    - name: source.zip
      location: output/artifacts
      pattern: source.zip
      shared: true
      required: true
  requirements:
    - extension: 'true'

triggers: []

branches:
  create: manually
  delete: never
  link-to-jira: true

labels: []
other:
  concurrent-build-plugin: system-default
