* {
    margin: 0;
    padding: 0;
    box-sizing: inherit;
}

html {
    box-sizing: border-box;
    font-size: 62.5%;

}

body {
    font-family: var(--font-primary);
    min-width: 32rem;
    min-height: 36rem;
    font-size: 2.5rem;
    font-weight: 400;
}
