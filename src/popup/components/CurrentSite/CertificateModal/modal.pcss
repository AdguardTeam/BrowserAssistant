.modal {
    outline: none;
    display: flex;
    flex-direction: column;
    justify-content: space-evenly;
    border-radius: 0.5rem;
    padding: 2.4rem 1.5rem 3.9rem 1.5rem;
    background-color: var(--white);
    border: 0.1rem solid var(--gray-border);
    width: 28.8rem;
    transform: translate(1.6rem, 13.5rem);
    box-shadow: 0 1rem 2rem rgba(0, 0, 0, 0.4);

    &__certificate {
        height: 20.6rem;

        &--expired {
            height: 29.6rem;
        }
    }


    &__secure-page {
        height: 8.7rem;
        transform: translate(1.6rem, 14.2rem);

        &--bank {
            height: 15rem;
        }
    }

    &__header {
        font-size: 1.6rem;
        margin-bottom: 0.9rem;
        color: var(--gray-base);
        line-height: 1;
        font-family: var(--font-primary);
    }

    &__text {
        font-family: var(--font-primary);
        font-size: 1.2rem;
        max-width: 100%;
        color: var(--gray88);

        &--expired {
            margin: 0.7rem 0;
            color: var(--red);
        }

        &--additional {
            max-width: 12.9rem;
        }

        &--warning {
            margin-bottom: 1.6rem;
        }

        &--uppercase {
            text-transform: uppercase;
        }

        &--link {
            cursor: pointer;
            text-decoration: underline;
        }

        &--notion {
            margin-top: 2.4rem;
            margin-bottom: 0.7rem;
        }

    }


    &__info {
        &--upper {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
        }

        &--upper > .global-switcher {
            width: auto;
        }

        &--lower .modal--text {
            background-color: red;
        }

    }
}

.arrow-up {
    z-index: var(--higher);
    width: 0;
    height: 0;
    border-left: var(--trinagle-border-side);
    border-right: var(--trinagle-border-side);
    border-bottom: 1rem solid var(--gray-border);
    position: absolute;
    top: -1rem;
    left: 6.6rem;

    &:after {
        content: '';
        border-left: var(--trinagle-border-side);
        border-right: var(--trinagle-border-side);
        border-bottom: 1rem solid var(--white);
        position: absolute;
        top: 0.15rem;
        left: -1rem;
    }
}
