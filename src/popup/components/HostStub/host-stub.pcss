.host-stub {
    font-size: 1.6rem;
    max-width: 28.8rem;
    padding: 1.6rem 0 1.6rem 1.6rem;
    display: flex;
    flex-direction: column;
}

button {
    border: 0;
    width: 3rem;
    height: 2rem;
    border-radius: 2rem;
}

.red {
    background-color: red;
}

.green {
    background-color: green;
}

.no-text .switcher__text {
    display: none;
}
